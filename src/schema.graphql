# import User from './generated/prisma.graphql'

type Query {
  user(username: String!): User
}

type Mutation {
  signupUser(username: String!): User!
  makePayment(amount: String!, sendUsername: String!, recipientUsername: String!, memo: String): Transaction
}

type Transaction {
  id: ID!
  memo: String
}